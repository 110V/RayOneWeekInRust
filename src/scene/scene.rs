use crate::hittable::{HittableList};

use super::Camera;

pub struct Scene{
    pub hittables:HittableList,
    pub camera:Camera,
}

impl Scene{
    pub fn new(){
        
    }
    pub fn add(){

    }
}